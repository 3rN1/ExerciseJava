// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {

        /* Get number Max & Min */
        //NumberMaxMin numberMaxMin = new NumberMaxMin();
        //numberMaxMin.getNumberMaxMin();

        /* Get multiplication Char for specific number */
        //MultiplicationChart multiplicationChart=new MultiplicationChart();
        //multiplicationChart.getMultiplicationChart();

        /* Get Palindrome words*/
        //Palindrome palindrome=new Palindrome();
        //palindrome.getPalindrome();

        /* Change positions for array values */
        //ChangePosArray changePosArray=new ChangePosArray();
        //changePosArray.changePosArrayValues();

        /* Get Fibonacci serie */
        //Fibonacci fibonacci=new Fibonacci();
        //fibonacci.getFibonnaci();

        /* Invert Array */
        //ArrayInverter arrayInverter=new ArrayInverter();
        //arrayInverter.getArrayInverter();

        /* Is Even Odd or Multiplicity */
        //EvenOddMultip evenOddMultip=new EvenOddMultip();
        //evenOddMultip.getEvenOddMultip();

        /* Options Menu */
        //Menu menu = new Menu();
        //menu.getMenu();

        /* Random Number */
        //RandomNumber randomNumber=new RandomNumber();
        //randomNumber.getRandomNumber();
    }
}